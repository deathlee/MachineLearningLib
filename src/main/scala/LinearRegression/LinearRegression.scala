package LinearRegression
import AbstractML._
class LinearRegression extends AbstractML{
    
        def train(trainPoints:List[L]):Unit={
          
        }
        def test(testPoints:List[L]):Unit={
          
        }
}